# [C] 2026-08-13 — Session log (overnight session, Wanda on iPhone)

## Shipped today — all merged to main

- **PR #6 + #7 — template upgrades.** Grafted from an external GEO/AEO skill comparison: Key Takeaways block, outbound citations (3–5, specific non-competing pages), claim-level reuse, the cut pass, Step-0 input routing in the YT→blog instructions. Salvaged from the retired Oct 2025 template Doc before deletion: YouTube embed rule, site-level linking habits (link new posts FROM an older post; sitemap check), `rel="sponsored"` on affiliate links. **The Oct 2025 "Blog Post Template 2025 SEO GEO AIO" Google Doc is cleared for deletion** — everything worth keeping is in; it still contains retired offers (Pick-My-Brain, Messaging Reset), so keeping it is a drift risk.
- **PR #8 — the `yt-to-blog` skill (v1).** Installable claude.ai skill: triggers on "repurpose this"/transcripts; offers ONLY from the live SEND A LINK sheet (stop-and-ask fallback); repo files win over bundled snapshots. Tested: 2 fixtures × with/without skill, 11 assertions each → 91% with vs 82% without; both baseline failures were offer-integrity (one snapshot-priced CTA, one fabricated "pulled live" claim). Also fixed the template's own H2-question vs Key-Takeaways/FAQ-heading wording tension.
- **PR #9 — answer-object image rules** (from Wanda's image/AI-search research, evidence hedges preserved): one complete-concept inline infographic for framework posts; text never depends on pixels; **cover = minimal text** (it doubles as the OG preview — Google advises against text-heavy preview images); caption field; crawlable `<img>` only; comparison-table preference in §6.
- **PR #11 — skill restructured to Wanda's architecture standard**: SKILL.md = directional router (file map + absolutes + 9-step pointer process) · references/ = blueprints (incl. new `output-format.md`) · `scripts/check_draft.py` = deterministic draft checker. Retest: 10/10 mechanical checks, independently re-verified; test agent ran the checker unprompted.

## Enforcement boundary (state it accurately)

Repo sessions: harness-enforced Stop hook (unchanged). The skill in plain chats: convention-enforced — Step 8 directs running `check_draft.py`, but no harness exists there to force it. Do not oversell.

## OPEN — waiting on Wanda's answer ("fix 1", "both", or "neither")

1. **Fix 1 (tiny):** one line in the skill — when the repo is present, save finished drafts into `deliverables/` so the existing Stop hook mechanically gates them. Currently the skill only prints to chat, which walks around the hook even in repo sessions.
2. **Fix 2 (test before trusting):** wire `skills/yt-to-blog/scripts/check_draft.py` into the Stop hook so blog-shaped deliverables get shape checks harness-enforced (gate on the PASTE-READY BODY marker so non-blog deliverables aren't misjudged).

## Wanda's to-dos when back at the desktop

- **Install the FINAL `.skill` file** — the LAST one sent in the chat (restructured version, sent ~1am). Earlier saves should be replaced by it.
- **Refresh Drive copies** in the AI-Ready Research Kit folder (Manage versions → Upload new version): `blog-post-template.md`, `draft-auditor-instructions.md`, `publishing-playbook.md` — use the latest files sent in chat (the last send supersedes the earlier ones).
- **Delete (or ARCHIVE-rename) the Oct 2025 template Doc.**
- Wanda mentioned unfinished work in **another Claude window** — contents unknown to this session; check open windows/sessions.
